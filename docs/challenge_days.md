🔥 День 1. Первый агент

Реализовать простого агента, который отвечает на вопросы и выводит это в вашем интерфейсе (простой чат, получение и отправка запросов через http клиент)

Результат: Агент принимает запрос и корректно вызывает инструмент
Пример: `python examples/day01_basic_agent.py "Что ты умеешь?"` → агент вызывает mock-инструмент и возвращает ответ.
Формат: Видео + Код (см. `examples/day01_basic_agent.py` + тест `tests/unit/examples/test_day01_basic_agent.py`)

🔥 День 2. Настройка AI

Привет, ангелы. Сегодня задание такое

- Научиться задавать формат результата для возвращения
- Задайте агенту формат возвращения в prompt
- Приведите пример формата возврата

Результат: Ответ от LLM можно распарсить
Формат: Код + Видео

**Реализация:**
- Пример: `examples/day02_output_schema.py` — агент запрашивает структурированный JSON ответ через `response_format: {"type": "json_object"}` и парсит его в `WeatherResponse` dataclass.
- Тест: `tests/unit/examples/test_day02_output_schema.py` — проверка построения промпта со схемой и парсинга JSON ответа.
- Запуск: `python examples/day02_output_schema.py "What is the weather like today?"`

🔥 День 3. Общение

- Задать ограничение модели, чтобы она сама остановилась
- Опишите в промпте результат, который модель должна собрать и выдать вам ответ

Как пример можно использовать данные для ТЗ (условно модель должна собрать требования для ТЗ и в какой-то момент переписки вернуть вам ТЗ)

Результат: Вы общаетесь и модель выдает вам какой-то результат на основе вашего общения (например, ТЗ)
Формат: Видео

**Реализация:**
- Пример: `examples/day03_conversation.py` — агент ведёт мультитурный диалог с LLM, собирая требования для ТЗ. Модель останавливается сама, когда выдаёт финальный JSON-спецификацию.
- Тест: `tests/unit/examples/test_day03_conversation.py` — проверка извлечения спецификации из ответа и управления историей диалога.
- Запуск: `python examples/day03_conversation.py "I want to build a task management system"`

🔥 День 4. Температура

Запустите один и тот же запрос с температурой = 0, 0.7 и 1.2

- Сравните результаты (точность, креативность, разнообразие)
- Сформулируйте, для каких задач лучше подходит каждая настройка

Результат: Текст или код с примерами разных ответов
Формат: Видео + Код

**Реализация:**
- Пример: `examples/day04_temperature.py` — агент запускает один запрос с температурами 0.0, 0.7, 1.2, сравнивает результаты и вычисляет метрику креативности (уникальность слов).
- Тест: `tests/unit/examples/test_day04_temperature.py` — проверка генерации с разными температурами и расчёта креативности.
- Запуск: `python examples/day04_temperature.py "Explain quantum computing"`

🔥 День 5. Версии моделей

- Вызовите один и тот же запрос на двух разных моделях (из начала, середины и конца списка HuggingFace)
- Замерьте время ответа, количество токенов и итоговую стоимость (если модель платная)
- Сравните качество ответов

Результат: Короткий вывод и ссылки

**Реализация:**
- Пример: `examples/day05_model_comparison.py` — агент запускает один запрос на нескольких моделях (gpt-3.5-turbo, gpt-4, mistral-7b-instruct), измеряет время ответа, токены, стоимость и оценивает качество.
- Тест: `tests/unit/examples/test_day05_model_comparison.py` — проверка измерения метрик, расчёта стоимости и сравнения моделей.
- Запуск: `python examples/day05_model_comparison.py "What is machine learning?"`

🔥 День 6. Chain of Thought (лайт)

- Возьмите простую задачу (например, арифметическую: «2x + 6 = 14» или логическую загадку)
- Сначала попросите модель дать ответ напрямую
- Затем в промпте добавьте инструкцию: «решай пошагово»

Сравните, отличаются ли ответы и какой из них оказался правильнее

Результат: Два ответа от модели (без CoT и с CoT), видно разницу
Формат: Видео + Код

**Реализация:**
- Пример: `examples/day06_chain_of_thought.py` — агент решает задачу дважды (прямой ответ и пошаговое решение с CoT), сравнивает результаты и проверяет правильность.
- Тест: `tests/unit/examples/test_day06_chain_of_thought.py` — проверка генерации прямого и CoT ответов, извлечения ответов и проверки правильности.
- Запуск: `python examples/day06_chain_of_thought.py "2x + 6 = 14"`

🔥День 7. Взаимодействие

Доброе утро, ангелы

- Создайте в коде два класса со своими моделями
- Настройте их взаимодействие
- Агент 1 делает задачу 1, выгружает результат в читаемом формате для агента 2 и тот на базе этого делает другую работу

Результат: Есть взаимодействие агентов, где второй проверяет работу первого
Формат: Код + Видео

**Реализация:**
- Пример: `examples/day07_agent_interaction.py` — два агента: `TaskAgent` (выполняет задачу) и `ReviewerAgent` (проверяет результат). `AgentOrchestrator` координирует их взаимодействие.
- Тест: `tests/unit/examples/test_day07_agent_interaction.py` — проверка выполнения задачи, форматирования для ревью, ревью результата и оркестрации обоих агентов.
- Запуск: `python examples/day07_agent_interaction.py "Write a function to calculate factorial"`

🔥 День 8. Работа с токенами

- Добавьте в код подсчёт токенов (на запрос и ответ)
- Сравните: короткий запрос, длинный запрос и запрос, превышающий лимит модели
- Реализуйте обрезку или сжатие текста (summary) перед отправкой

Результат: Код, который считает токены и показывает, как меняется поведение модели
Формат: Видео + Код

**Реализация:**
- Пример: `examples/day08_token_handling.py` — агент использует `TokenCounter` для подсчёта токенов в коротких/длинных/переполняющих запросах, реализует обрезку и сжатие текста при превышении лимита.
- Тест: `tests/unit/examples/test_day08_token_handling.py` — проверка подсчёта токенов, обрезки и сжатия текста.
- Запуск: `python examples/day08_token_handling.py`

🔥 День 9. Подключение MCP

Установите MCP SDK/клиент (или поднимите MCP-сервер, если используете локальный вариант)

Напишите минимальный код, который создаёт MCP-соединение и получает от него список доступных инструментов
Результат: Код, который показывает список инструментов MCP

Формат: Видео + Код

**Реализация:**
- Пример: `examples/mcp/basic_discovery.py` — демонстрация подключения к MCP и получения списка доступных инструментов через `MCPClient.discover_tools()`. Показывает вызов инструментов (`add`, `count_tokens`).
- Тест: MCP-клиент используется в интеграционных тестах (`tests/integration/presentation/mcp/`).
- Запуск: `python examples/mcp/basic_discovery.py` (требуется запущенный MCP-сервер).
- См. также: `src/presentation/mcp/client.py` — реализация MCP-клиента, `src/presentation/mcp/tools/` — примеры инструментов.

🔥 День 10. Первый инструмент MCP

- Реализуйте простой инструмент (например, яндекс трекер) и зарегистрируйте его в MCP
- Подключите агента, чтобы он мог вызвать этот инструмент через MCP
- Получите результат (например количество открытых тасок)

Результат: Агент делает вызов к вашему инструменту и получает результат
Формат: Видео + Код

**Реализация:**
- Примеры инструментов: `src/presentation/mcp/tools/` — различные MCP-инструменты, зарегистрированные через декоратор `@mcp.tool()`.
- Простой пример: `src/presentation/mcp/tools/nlp_tools.py` — инструмент `parse_intent` для распознавания намерений пользователя.
- Каналы: `src/presentation/mcp/tools/channels/` — инструменты для управления каналами Telegram (digest, metadata, posts).
- Регистрация: Инструменты автоматически регистрируются через `tools_registry.py` и доступны через `MCPClient.discover_tools()`.
- Демонстрация: `examples/mcp/basic_discovery.py` показывает вызов инструментов (`add`, `count_tokens`).
- Запуск: Запустите MCP-сервер (`make mcp-server-start`), затем используйте `MCPClient` для вызова инструментов.

🔥 День 11. Планировщик + MCP

- Сделайте инструмент «reminder» (например, сохраняет задачи в JSON или SQLite)
- Инструмент должен раз в какое-то время уведомлять вас о каком-то summary

Результат: У вас есть агент работающий 24/7 и выдающий вам сводку
Формат: Видео + Код

**Реализация:**
- Worker Pipeline: В проекте реализована система фоновых workers, работающих 24/7 (`src/workers/`).
- `PostFetcherWorker` (`src/workers/post_fetcher_worker.py`): Собирает посты из Telegram-каналов по расписанию (каждый час), использует MCP для вызова инструментов `collect_posts` и `save_posts_to_db`. Запускается через `src/workers/post_fetcher_worker_main.py`.
- `CacheRefreshWorker` (`src/workers/cache_refresh_worker.py`): Обновляет кэш диалогов ежедневно, поддерживает quiet hours (не шумит ночью).
- Планировщик: `src/workers/schedulers.py` предоставляет функции `is_quiet_hours()` и `is_time_to_send()` для управления расписанием.
- Запуск: `python -m src.workers.post_fetcher_worker_main` или через `make day-11-up` / `make day-12-up`.
- Мониторинг: Workers пишут метрики в Prometheus (`post_fetcher_worker_running`, `post_fetcher_duration_seconds`, `post_fetcher_posts_saved_total`).
- Пример reminder: Можно создать MCP-инструмент `reminder` в `src/presentation/mcp/tools/`, который сохраняет задачи в MongoDB/JSON и отправляет сводки через Telegram по расписанию (см. `PostFetcherWorker` как референс).

🔥 День 12. Композиция MCP-инструментов

- Создайте несколько инструментов (например: searchDocs, summarize, saveToFile)
- Реализуйте агента, который через MCP связывает их в цепочку: ищет → суммаризирует → сохраняет результат

Результат: Автоматическая связка инструментов MCP в пайплайн
Формат: Видео + Код

**Реализация:**
- Композиция инструментов: В проекте реализован пайплайн генерации дайджестов через цепочку MCP-инструментов (`src/presentation/mcp/tools/channels/`).
- Пайплайн: `get_posts_from_db` → `summarize_posts` → `format_digest_markdown` → `combine_markdown_sections` → `convert_markdown_to_pdf` / `save_posts_to_db`.
- Оркестратор: `parse_intent` (`src/presentation/mcp/tools/nlp_tools.py`) распознаёт намерение "digest" и автоматически вызывает цепочку инструментов через `mcp_tools` параметр.
- Инструменты:
  - `get_posts_from_db` / `collect_posts` (`posts_management.py`) — поиск и сбор постов из каналов
  - `summarize_posts` (`channel_digest.py`) — суммаризация постов через LLM
  - `format_digest_markdown` (`channel_digest.py`) — форматирование в Markdown
  - `combine_markdown_sections` (`channel_digest.py`) — объединение секций
  - `convert_markdown_to_pdf` (`channel_digest.py`) — конвертация в PDF
  - `save_posts_to_db` (`posts_management.py`) — сохранение в MongoDB
- Использование: Через `MCPClient.call_tool()` можно вызывать инструменты последовательно, передавая результаты предыдущего шага следующему.
- Пример: `src/presentation/mcp/tools/channels/channel_digest.py::generate_channel_digest()` показывает композицию инструментов для генерации дайджеста.
- CLI: `poetry run python -m src.presentation.cli.backoffice.main digest run --user-id 42 --hours 24` использует этот пайплайн.

🔥 День 13. Environment

Задача: Соединить агента с реальным окружением, заставить поднимать докер (как вариант эмулятор) и запускать в нем что-то

Результат: Вы можете как итог своей работы что-то проверить на реальном девайсе
Формат: Видео + Код

**Реализация:**
- Shared Infrastructure Bootstrap: В проекте реализован автоматический bootstrap общих сервисов через `scripts/ci/bootstrap_shared_infra.py`.
- Make targets: `make day-23-up` поднимает все сервисы (MongoDB, Prometheus, Grafana, Loki, mock LLM), `make day-23-down` останавливает их.
- Команды:
  - `make day-23-up` — запуск shared infrastructure
  - `make day-23-down` — остановка shared infrastructure
  - `scripts/ci/bootstrap_shared_infra.py --check` — проверка статуса сервисов без запуска (для CI)
  - `make day-23-ps` — показать статус контейнеров
- Smoke test: После `make day-23-up` можно проверить здоровье через:
  - MongoDB: `docker exec -it shared-mongo mongosh --eval "db.adminCommand('ping')"`
  - Prometheus: `curl http://localhost:9090/-/healthy`
  - Grafana: `curl http://localhost:3000/api/health`
  - Loki: `curl http://localhost:3100/ready`
- Документация: `docs/specs/epic_23/shared_infra_restart.md` описывает процесс bootstrap/teardown и валидацию restart.
- Интеграционные тесты: `tests/integration/shared_infra/test_bootstrap.py` проверяет bootstrap, cleanup и restart validation.
- CI/CD: GitHub Actions использует `bootstrap_shared_infra.py --check` для gating перед тестами.

🔥День 14. Анализ проекта

Возьмите большой проект (например, свой код или чей-то пет проект)
Отправьте код агенту и попросите найти очевидные баги или построить структуру проекта

Результат: Агент анализирует весь код и выдает результат
Формат: Видео + Код

**Реализация:**
- Multi-Pass Code Review: В проекте реализован 3-проходный анализатор кода через MCP-инструмент `review_homework` (`src/presentation/mcp/tools/homework_review_tool.py`).
- Проходы: Pass 1 (Static Analysis — Flake8, Pylint, MyPy), Pass 2 (LLM Code Review — анализ кода и тестов), Pass 3 (LLM Synthesis — финальный синтез), Pass 4 (Log Analysis — группировка и классификация ошибок).
- MCP Tool: `@mcp.tool()` декоратор регистрирует `review_homework`, который принимает `repo_path`, `commit_hash`, `student_id` и возвращает структурированный отчёт.
- Использование: Через Telegram бота (`/review <commit_hash>`) или напрямую через MCP-клиент.
- Вывод: Структурированный JSON с анализом кода, списком проблем, рекомендациями и метаданными (токены, время выполнения, feature flags).
- Тесты: `tests/integration/presentation/mcp/test_homework_review_tool.py` проверяет все проходы анализатора.
- Документация: `docs/specs/epic_14/` описывает архитектуру multipass reviewer.

🔥 День 15. Сжатие диалога

Реализуйте механизм «сжатия истории диалога» (например, каждые 10 сообщений делать summary и хранить его вместо оригинала)
Проверьте, как агент продолжает вести разговор с учётом summary вместо всей истории
Сравните качество ответов и использование токенов

Результат: Агент работает с компрессией и выполняет ту же работу за меньшее количество токенов
Формат: Видео + Код

**Реализация:**
- Compression Utility: В проекте реализован механизм сжатия больших JSONL файлов через `scripts/tools/compress_jsonl.py`.
- JSONL Compression: Скрипт использует gzip для сжатия больших файлов (>500KB), сохраняя оригиналы и создавая `.jsonl.gz` версии.
- Token Budgeting: `docs/operational/context_limits.md` описывает стратегии управления токенами и сжатия контекста для разных ролей.
- Dialog Context: `src/domain/interfaces/dialog_context_repository.py` определяет интерфейс для персистентного хранения контекста диалогов с возможностью сжатия.
- Использование:
  - `python scripts/tools/compress_jsonl.py results_stage20.jsonl` — сжать JSONL файл
  - `gunzip results_stage20.jsonl.gz` — распаковать для использования
- Примеры: `results_stage20.jsonl.gz` и `results_with_labels.jsonl.gz` сжаты с 555KB до ~148KB (26.5% от оригинала).
- Документация: `docs/roles/analyst/examples/day_15_compression.md` и `docs/roles/analyst/examples/day_8_token_budgeting.md` описывают стратегии компрессии.

🔥 День 16. Внешняя память

Подключите «внешнюю память» для модели: сохранение промежуточных результатов в SQLite/JSON-файл
Проверьте, что данные сохраняются и доступны между запусками

Результат: Агент с долговременной памятью (SQLite/JSON)
Формат: Видео + Код

**Реализация:**
- Dialog Context Repository: В проекте реализовано персистентное хранение контекста диалогов через `src/domain/interfaces/dialog_context_repository.py`.
- MongoDB Persistence: `src/infrastructure/repositories/mongo_dialog_context_repository.py` сохраняет контекст в MongoDB с поддержкой метаданных и временных меток.
- Session Management: `src/infrastructure/dialogs/session_manager.py` управляет сессиями пользователей с автоматическим сохранением/загрузкой контекста.
- Метрики: `butler_context_operations_total` в `src/infrastructure/metrics/butler_metrics.py` отслеживает операции `get_context` и `save_context`.
- Benchmark Data Persistence: `src/infrastructure/repositories/mongo_benchmark_repository.py` сохраняет benchmark данные (digests, review_reports) для последующего экспорта.
- Использование: Контекст автоматически сохраняется после каждого диалога через Butler Bot, доступен между перезапусками через MongoDB.
- Тесты: `tests/test_session_manager.py` проверяет сохранение и загрузку контекста.

🔥 День 17. Пайплайн обработки данных

Постройте простой пайплайн, например, входной текст → очистка (удаление стоп-слов/HTML) → сжатие (summary) → запрос к модели (поиск по документации)
Автоматизируйте это в виде функции или CLI-утилиты
Прогоните несколько разных текстов и сравните результаты

Результат: Мини-пайплайн для подготовки данных перед LLM
Формат: Видео + Код

**Реализация:**
- Benchmark Data Pipeline: В проекте реализован пайплайн обработки данных для бенчмарков через `scripts/quality/analysis/`.
- Пайплайн: `ingest` (seed_benchmark_data.py) → `process` (внутренняя обработка) → `export` (export_digests.py, export_review_reports.py) → `validate` (verify_benchmark_counts.py).
- Скрипты:
  - `seed_benchmark_data.py` — загрузка данных в MongoDB (synthetic/recovered)
  - `export_digests.py` — экспорт digests в JSONL
  - `export_review_reports.py` — экспорт review reports в JSONL
  - `verify_benchmark_counts.py` — валидация покрытия данных
- Автоматизация: CI/CD (`github/workflows/ci.yml`) автоматически запускает пайплайн при изменениях в данных.
- Use Cases: `src/application/benchmarking/seed_benchmark_data.py` реализует логику ингеста с валидацией и метриками.
- CLI: `python scripts/quality/analysis/seed_benchmark_data.py --channels onaboka,alexgladkovblog` — запуск пайплайна.
- Документация: `docs/reference/en/PERFORMANCE_BENCHMARKS.md` описывает структуру пайплайна и формат данных.

🔥 День 18. Реальная задача

- Придумайте любую задачу реальную (как вариант вам - выложить в стор приложение, завести хостинг)
- Сделайте приложение, которое будет делать эту задачу

Результат: Есть пайплайн (он может не работать из-за ключей)
Формат: Видео + Код
При заполнении укажите какую задачу вы сделали

**Реализация:**
- Epic 23: Observability & Benchmark Enablement — реальная задача, реализованная в `docs/specs/epic_23/`.
- Задача: Внедрение observability (метрики, логи, трейсинг) и enablement бенчмарков для оценки качества системы.
- Реализованные компоненты:
  - Prometheus метрики (`structured_logs_total`, `benchmark_export_duration_seconds`, `shared_infra_bootstrap_status`, `rag_variance_ratio`)
  - Grafana dashboards (`grafana/dashboards/stage05-benchmarks.json`) с Epic 23 панелями
  - Loki alerts (`prometheus/alerts.yml`) для мониторинга ошибок и backlog
  - Shared infrastructure automation (`make day-23-up/down`, `scripts/ci/bootstrap_shared_infra.py`)
  - Benchmark data pipeline (seeding, export, validation)
  - RAG++ reproducibility (seed, variance metrics, canary rollout plan)
- Production-ready: Все компоненты работают в production через CI/CD, мониторинг активен, бенчмарки автоматизированы.
- Документация: `docs/specs/epic_23/tech_lead_plan.md`, `docs/specs/epic_23/acceptance_matrix.md`, `docs/specs/epic_23/work_log.md`.
- Доказательства: `docs/specs/epic_23/stage_05_03_pilot_log.md`, `docs/specs/epic_23/shared_infra_restart.md`.

🔥 День 19. Индексация документов

Возьмите набор документов (README, статьи, код или pdf → текст)
Реализуйте пайплайн: разбивка текста на чанки + генерация эмбеддингов
Сохраните индекс (например, в FAISS, SQLite с векторами или просто JSON)

Результат: Локальный индекс документов с эмбеддингами
Формат: Видео + Код

**Реализация:**
- Embedding Index Pipeline: В проекте реализован пайплайн индексации документов через `src/application/embedding_index/BuildEmbeddingIndexUseCase` и CLI `src/presentation/cli/embedding_index.py`.
- Пайплайн: `collect` (FilesystemCollector) → `preprocess` (TextPreprocessor) → `chunk` (SlidingWindowChunker) → `embed` (LocalEmbeddingClient) → `index` (MongoDocumentRepository + Redis/FAISS).
- CLI: `poetry run python -m src.presentation.cli.backoffice.main index run` — запуск индексации для всех источников из конфигурации.
- Хранилища: MongoDB для метаданных документов и чанков, Redis/FAISS для векторного поиска, fallback pickle индекс (`var/indices/embedding_index_v1.pkl`).
- Компоненты:
  - `src/infrastructure/embedding_index/collectors/filesystem_collector.py` — сбор документов из файловой системы
  - `src/infrastructure/embedding_index/preprocessing/text_preprocessor.py` — предобработка текста
  - `src/infrastructure/embedding_index/chunking/sliding_window_chunker.py` — разбивка на чанки с overlap
  - `src/infrastructure/embeddings/LocalEmbeddingClient` — генерация эмбеддингов
  - `src/infrastructure/embedding_index/repositories/mongo_document_repository.py` — сохранение в MongoDB
- Инспекция: `poetry run python -m src.presentation.cli.backoffice.main index inspect` показывает статистику индекса (количество документов, чанков, размер).
- Документация: `docs/specs/epic_19/` описывает архитектуру индексации (Stage 19.04 runbook).

🔥 День 20. Первый RAG-запрос

Реализуйте функцию: «вопрос → поиск релевантных чанков → объединение с вопросом → запрос к LLM»
Сравните ответ модели с RAG и без RAG
Сделайте вывод: где RAG помог, где — нет

Результат: Агент с двумя режимами (с RAG / без RAG) и сравнением
Формат: Видео + Код

**Реализация:**
- RAG Comparison Demo: В проекте реализован интерактивный demo для сравнения RAG vs non-RAG через `scripts/rag/day_20_demo.py`.
- Use Case: `src/application/rag/CompareRagAnswersUseCase` реализует логику сравнения двух режимов (с RAG и без RAG) для одного вопроса.
- Пайплайн RAG: `Query` → `RetrievalService` (поиск релевантных чанков) → `PromptAssembler` (объединение с вопросом) → `LLMServiceAdapter` (запрос к LLM).
- Demo: `python scripts/rag/day_20_demo.py` запускает интерактивный консольный demo с предустановленными вопросами и персонами ("Обычный бот", "Ехидный дедушка").
- Вывод: Demo показывает оба ответа (с RAG и без RAG) с метриками (токены, время выполнения) и позволяет оценить, где RAG улучшил качество ответа.
- CLI: `poetry run python -m src.presentation.cli.backoffice.main rag compare --question "Your question"` — программное сравнение.
- Документация: `docs/reference/en/PERFORMANCE_BENCHMARKS.md` содержит результаты бенчмарков RAG vs non-RAG.
- Метрики: `rag_rerank_score_variance`, `rag_variance_ratio` отслеживают качество RAG-ответов (TL-07).

🔥 День 21. Реранкинг и фильтрация

Добавьте второй этап после поиска: reranker или фильтр релевантности (например, через порог коэффициента похожести или используя стороннюю модель)
Сравните качество ответа без фильтра и с фильтром

Настройте порог отсечения нерелевантных результатов

Результат: Улучшенный RAG с фильтрацией/реранкингом
Формат: Видео + Код

**Реализация:**
- RAG++ (Reranking & Filtering): В проекте реализован расширенный RAG с реранкингом и фильтрацией через `scripts/rag/day_21_demo.py` и конфигурацию `config/retrieval_rerank_config.yaml`.
- Компоненты:
  - `ThresholdFilterAdapter` (`src/infrastructure/rag/threshold_filter_adapter.py`) — фильтрация по порогу similarity score
  - `LLMRerankerAdapter` (`src/infrastructure/rag/llm_reranker_adapter.py`) — реранкинг через LLM с seed для reproducibility
  - `VectorSearchAdapter` (`src/infrastructure/rag/vector_search_adapter.py`) — векторный поиск с headroom multiplier
- Конфигурация: `config/retrieval_rerank_config.yaml` содержит настройки `seed`, `variance_window`, `adaptive_threshold` для контроля реранкинга.
- Demo: `python scripts/rag/day_21_demo.py --persona 1 --filter --threshold 0.5 --reranker llm` — запуск с кастомными параметрами фильтрации и реранкинга.
- Метрики: `rag_rerank_score_variance`, `rag_variance_ratio`, `rag_fallback_reason_total` отслеживают качество и стабильность реранкинга (TL-07).
- Стратегии: `reranker.strategy` может быть `off`, `llm`, `cross_encoder` (cross_encoder fallback на off).
- Документация: `docs/specs/epic_23/owner_only/rag_plus_plus_addendum.md` описывает tuning cadence, randomness controls, variance metrics, canary rollout plan.
- Тесты: `tests/integration/rag/test_variance_metrics.py` проверяет метрики variance и fallback.

🔥 День 22. Цитаты и источники

Доработайте свой RAG так, чтобы модель обязательно возвращала цитаты/ссылки на источники из базы
Протестируйте на 5 вопросах: есть ли ссылки в каждом ответе
Оцените, стало ли меньше «галлюцинаций»

Результат: Ответы модели с обязательными ссылками на источники
Формат: Видео + Код

**Реализация:**
- Citations in RAG: В проекте реализована поддержка цитат и источников через `RetrievedChunk` (`src/domain/rag/value_objects.py`) с обязательными полями `document_id`, `source_path`, `metadata`.
- Структура: `RetrievedChunk` содержит:
  - `chunk_id` — идентификатор чанка
  - `document_id` — идентификатор родительского документа
  - `text` — текст чанка
  - `similarity_score` — оценка релевантности
  - `source_path` — путь к исходному документу (для цитат)
  - `metadata` — дополнительные метаданные (page, section, etc.)
- Prompt Assembly: `PromptAssembler` (`src/application/rag/prompt_assembler.py`) включает источники в промпт для LLM, указывая `source_path` и `document_id` для каждого чанка.
- Валидация: `RetrievedChunk.__post_init__` проверяет наличие обязательных полей (`document_id`, `source_path`).
- Использование: RAG-ответы автоматически включают источники из `RetrievedChunk.source_path`, что позволяет модели цитировать документы и уменьшает галлюцинации.
- Документация: `docs/roles/analyst/examples/day_22_rag_citations.md` описывает использование цитат в RAG-ответах.
- Тесты:
  - `tests/integration/rag/test_citations_enforcement.py` — integration тесты для проверки включения citations в RAG промпты (source_path входит в промпт для каждого чанка, проверка формата цитат)
  - `tests/integration/rag/test_variance_metrics.py` использует `RetrievedChunk` с полными метаданными (document_id, source_path, metadata) для проверки валидации
- Policy: Рекомендуется всегда включать `source_path` и `document_id` в `RetrievedChunk` для обеспечения traceability ответов. `PromptAssembler` автоматически включает `source_path` в промпт через шаблон `_CHUNK_TEMPLATE` (строка 54: "Источник: {source_path}").

🔥 День 23. Свободный день

Сделайте что-нибудь

Результат: Укажите в комментариях  
Формат: Видео + Код

**Реализация (EP23 контекст):**
- Epic 23 · Observability & Benchmark Enablement:
  - Реализованы метрики и алерты (`structured_logs_total`, `benchmark_export_duration_seconds`, `shared_infra_bootstrap_status`, `rag_variance_ratio`, Loki алерты) — см. `src/infrastructure/metrics/observability_metrics.py`, `prometheus/alerts.yml`, `grafana/dashboards/stage05-benchmarks.json`.
  - Автоматизирован bootstrap shared infra (`make day-23-up/down`, `scripts/ci/bootstrap_shared_infra.py --check`) и добавлены интеграционные тесты (`tests/integration/shared_infra/test_bootstrap.py`).
  - Построен полный пайплайн бенчмарков: сидинг (`SeedBenchmarkDataUseCase` + `scripts/quality/analysis/seed_benchmark_data.py`), экспорт (`export_digests.py`, `export_review_reports.py`), валидация (`verify_benchmark_counts.py`).
  - RAG++ получил управляемую вариативность и воспроизводимость (`config/retrieval_rerank_config.yaml`, `src/infrastructure/rag/llm_reranker_adapter.py`, `docs/specs/epic_23/owner_only/rag_plus_plus_addendum.md`).

👉 Для новых дней Epic 23 даёт эталон по observability/инфраструктуре: любые следующие фичи (например, голосовой агент) должны сразу экспортировать метрики и использовать shared infra.

---

🔥 День 24. Голосовой агент (Speech → LLM → Text)

Соедините модуль распознавания речи с LLM:

- Вход: голосовое сообщение в Telegram.  
- Шаг 1: голос → распознаётся в текст (STT, локальная модель/движок, оффлайн).  
- Шаг 2: бот присылает пользователю распознанный текст и просит подтверждение.  
- Шаг 3: после подтверждения текст отправляется в LLM/Butler‑пайплайн для выполнения команды.  
- Выход: текстовый ответ (для MVP) и, при необходимости, запуск существующих use case’ов (например, генерация дайджеста).

Примеры запросов:
- «Сделай дайджест по каналу X за последние 24 часа»
- «Посчитай, сколько…»
- «Дай определение…»
- «Скажи анекдот»

Результат: Голосовое сообщение → распознанный текст → подтверждённый запрос → выполненный сценарий (например, дайджест).  
Формат: Видео + Код

**Планируемая реализация (MVP, без деталей):**
- **Инфраструктура (STT):**
  - Добавить интерфейс `SpeechToTextService` в доменный слой (`src/domain/interfaces/speech_to_text_service.py`) c методом вида `transcribe(audio: bytes) -> str`.
  - Реализация в инфраструктуре (`src/infrastructure/stt/local_speech_to_text_adapter.py`) на базе локального STT‑движка (например, Whisper/Vosk), строго оффлайн.
- **Use case:**
  - Новый use case `ProcessVoiceCommandUseCase` в application слое, который:
    1. Принимает аудио (из Telegram update) и метаданные пользователя.
    2. Вызывает `SpeechToTextService` → получает текст команды.
    3. Отправляет пользователю текст для подтверждения (через существующий Telegram/Butler слой).
    4. После подтверждения маршрутизирует текст в уже существующий LLM/Butler сценарий (например, в digest‑use case).
- **Интеграция с Telegram‑ботом:**
  - Расширить текущего бота: обработчик voice‑сообщений вызывает `ProcessVoiceCommandUseCase`.
  - Для MVP: поддерживать хотя бы один реальный сценарий — «Сделай дайджест» на базе уже реализованных digest use cases.

Поддерживаемый язык: **русский**, latency для MVP не критичен; данные (аудио/транскрипты) пока не сохраняются в Mongo, используются только в оперативном пайплайне.
